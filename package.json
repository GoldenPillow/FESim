{
  "name": "fesim",
  "private": true,
  "packageManager": "pnpm@11.21.0",
  "type": "module",
  "scripts": {
    "build": "pnpm -C apps/web build",
    "deploy": "pnpm run build && npx wrangler deploy"
  }
}
